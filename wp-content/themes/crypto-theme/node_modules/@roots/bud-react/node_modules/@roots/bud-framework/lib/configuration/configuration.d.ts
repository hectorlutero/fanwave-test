import type { Bud } from '@roots/bud-framework';
import type { File } from '@roots/bud-framework/context';
/**
 * User config parser
 */
declare class Configuration {
    bud: Bud;
    /**
     * Class constructor
     */
    constructor(bud: Bud);
    /**
     * Process dynamic configuration
     */
    dynamicConfig(config: (bud: Bud) => Promise<any>): Promise<void>;
    handleConfigEntry(obj: any, [key, value]: [string, unknown]): Promise<void>;
    /**
     * Process configuration
     */
    run(file: File): Promise<void>;
    /**
     * Process static configuration
     */
    staticConfig(config: Record<string, any>): Promise<unknown>;
}
export default Configuration;
