export interface Props {
    color: string;
    label: string;
    message: string;
}
export declare const Message: ({ color, label, message }: Props) => import("@roots/bud-support/jsx-runtime").JSX.Element;
