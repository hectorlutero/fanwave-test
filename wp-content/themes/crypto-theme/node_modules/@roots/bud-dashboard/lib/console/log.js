import { jsx as _jsx } from "@roots/bud-support/jsx-runtime";
import { Message } from './message.js';
export const Stderr = ({ message }) => (_jsx(Message, { color: "red", label: "stderr", message: message }));
export const Stdout = ({ message }) => (_jsx(Message, { color: "green", label: "stdout", message: message }));
export const Log = ({ message, stream }) => {
    switch (stream) {
        case `stdout`:
            return _jsx(Stdout, { message: message });
        case `stderr`:
            return _jsx(Stderr, { message: message });
    }
};
