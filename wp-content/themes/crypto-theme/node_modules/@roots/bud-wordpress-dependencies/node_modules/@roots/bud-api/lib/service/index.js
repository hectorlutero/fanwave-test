import { ServiceContainer } from '@roots/bud-framework/service';
import * as methods from '../methods/index.js';
/**
 * Bud.API {@link ServiceContainer}
 */
class Api extends ServiceContainer {
    /**
     * {@link BudApi.label}
     */
    label = `api`;
    /**
     * {@link ServiceContainer.bootstrap}
     */
    async bootstrap(bud) {
        Object.entries(methods).map(([k, v]) => bud.bindFacade(k, v, bud));
    }
}
export { Api as default };
