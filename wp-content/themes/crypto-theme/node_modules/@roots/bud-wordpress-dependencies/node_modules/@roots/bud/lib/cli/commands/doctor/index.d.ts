import type { Bud } from '@roots/bud';
import type { Extension } from '@roots/bud-framework/extension';
import BudCommand from '@roots/bud/cli/commands';
import webpack from '@roots/bud-support/webpack';
/**
 * bud doctor command
 */
export default class DoctorCommand extends BudCommand {
    static paths: string[][];
    static usage: import("@roots/bud-support/clipanion").Usage;
    configuration: Bud[`build`][`config`];
    disabledExtensions: Array<[string, Extension]>;
    enabledExtensions: Array<[string, Extension]>;
    entrypoints: Array<[string, webpack.EntryObject]>;
    makeTimer: () => () => string;
    resolvedDependencies: Record<string, string>;
    silent: boolean;
    timings: Record<string, string>;
    /**
     * Execute command
     */
    execute(): Promise<void>;
    mapExtensions(extensions: Array<[string, Extension]>): import("@roots/bud-support/jsx-runtime").JSX.Element[];
    packageCheck(signifier: string): Promise<import("@roots/bud-support/jsx-runtime").JSX.Element>;
    seconds(hrTime: [number, number]): string;
}
